import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.icecream.shop.game',
  appName: 'IceCream Shop Game',
  webDir: 'dist'
};

export default config;
